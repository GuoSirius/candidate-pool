node_modules/
npm-debug.log*
.DS_Store
*.log

# 临时产物（CI / 本地测试生成，不入库）
reports/_*.html
data/_*.json

# 尾盘选股运行产物（行业映射缓存之外的日度数据与报告，不入库）
eod/data/eod-*.json
eod/reports/

# 推送凭据（含 163 授权码 / 微信 key，切勿提交）
notify_config.json

# Cloudflare D1 凭据（含 account_id / database_id / API token，切勿提交）
db/.env

# 本地数据库文件（若改成本地 sqlite 调试，勿入库）
*.sqlite
*.sqlite3
*.db
db/local.db*

# 运行时/自动化本地目录
.workbuddy/

# Wrangler 本地状态（缓存 / 构建临时产物 / 本地 D1 数据，勿入库）
.wrangler/
.dev.vars
.dev.vars.*
