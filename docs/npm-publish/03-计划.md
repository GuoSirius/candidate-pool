# 独立发布 · 实施计划（方案 A：全量打包 + GitHub 自动发布）

> **一句话**：本机只负责 `bump 版本 → 写 CHANGELOG → 提交 → 打 tag → push`，`npm publish` 与 GitHub Release **全部在 CI 完成**；发布前必过「版本一致 / 离线自测 / 打包体检」三道闸。
>
> 前置方案对比见 [02-方案.md](02-方案.md)（三方案取舍与边界清单）。

## 1. 已定策略

| 决策 | 结论 | 依据 |
|------|------|------|
| 打包范围 | **方案 A：全量打包**（根 + `eod/` + `db/`） | 尾盘脚本跨目录依赖仓库根（`../notify`、`../../db/*`），「只发尾盘」要把依赖内收 → 结构分叉，代价大于收益 |
| 发布执行者 | **GitHub Actions**，由 tag 推送触发 | 与 `yyds` / `change-image-suffix` / `uuid-nanoid` / `enhance-axios` 一致；本机不必持有 npm 凭据（`yyds` 里那句注释就是这个意思：*Publishing happens here (CI), not in the local `npm run release`*） |
| Release 说明 | `CHANGELOG.md` 本版本段落，**取不到则用 `git log` 兜底** | 满足「每个版本的所有提交变化都可见」；自动 notes 只列有 PR 的变更，本仓库直接推 main → 会只剩一行 compare 链接 |
| 包名 | `candidate-pool`（公开包） | `npm view` 实测未占用；仓库本身已公开，发 npm 不新增暴露面 |
| 包入口 | `bin/` 两个转发入口，默认工作目录 = **当前目录** | `paths.js` 的默认 HOME 是包根（为保持仓库内运行行为不变），装包后必须改指 cwd，否则产物写进 `node_modules` |

## 2. 交付物（已完成）

| 文件 | 作用 |
|------|------|
| `bin/candidate-pool.js`、`bin/tail-screener.js` | npm 入口；把默认工作目录从「包目录」改成「当前目录」，`CANDIDATE_POOL_HOME` / `--cwd` 优先 |
| `package.json` 的 `bin` / `files` / `repository` / `publishConfig` | 声明入口、白名单、仓库地址、公开发布 |
| `.github/workflows/release.yml` | tag 触发的发布工作流（见 §4） |
| `scripts/release-notes.mjs` | 生成 Release 说明：CHANGELOG 段落优先、git 兜底，附 compare 链接 + 提交数 + 安装命令 |
| `scripts/verify-pack.mjs` | 打包体检：敏感文件守门 + 必需文件点名 + 体积红线 + 守门规则自检 |
| `eod/lib/sector.js` | 行业映射加了「工作目录 → 包内种子」两级读取，避免 npx 场景全塌成「未分类」 |
| `changelog.config.cjs` | **删掉手写的 `repo` 对象**（见 §6 的 81 个死链） |
| `CHANGELOG.md` | 修掉 81 个 `https://undefined/...` 死链 |

## 3. 需要你做的一步：`NPM_TOKEN`

CI 发布需要仓库 Secret，本机不放任何凭据：

1. <https://www.npmjs.com/settings/~tokens> → Generate New Token → **Automation**
   （必须是 Automation 类型；Publish 类型在 CI 里会因 2FA 失败）
2. GitHub 仓库 → Settings → Secrets and variables → Actions → New repository secret
   名称填 **`NPM_TOKEN`**，值贴上面的令牌
3. 若日后改用 scoped 包名（`@guosirius/candidate-pool`），需同步改 `package.json` 的 `name`，并把 `publishConfig.access` 保持 `public`

## 4. 发布链路

```
本机  npm run release                      ← 只有这一条命令，跑完即「全部流程已启动」
      ├─ 类型门禁 → 未提交检测 → 选 patch/minor/major
      ├─ changelogen --bump（改 package.json 版本 + 增量写 CHANGELOG.md）
      ├─ 同步 worker/web 子包版本
      ├─ ⑥ **打印 Release 说明预览**（与 CI 同一个脚本，所见即所得；为空则当场告警）
      ├─ 提交 chore(release): vX.Y.Z → git tag vX.Y.Z → push 分支与 tag
      ├─ 打印三条链接：Actions 运行日志 / npm 包页 / Release 页
      └─ 部署 Cloudflare（沿用原有步骤）
              ↓ tag 推送触发（无需本机再做任何事）
CI    .github/workflows/release.yml
      ├─ ① 校验 tag 与 package.json 版本一致
      ├─ ② npm ci
      ├─ ③ npm run selftest（离线：路径 41 + 时段 60 + 结构同步 19 项）
      ├─ ④ npm run verify:pack（打包体检）
      ├─ ⑤ npm publish --provenance --access public
      ├─ ⑥ 生成 Release 说明 → softprops/action-gh-release@v2
      └─ ⑦ 往 Actions 摘要写「包名 / 版本 / npm 链接 / Release 链接」
```

**本机永不执行 `npm publish`**；本机命令的职责就是「把 tag 推上去并告诉你去哪看结果」，因此它会把
Actions / npm / Release 三条链接直接打出来（约 1~2 分钟出结果，标红最常见原因是没配 `NPM_TOKEN`）。

### `npm run release` 与 `npm run release:notes` 的区别

| 命令 | 做什么 | 会发布吗 |
|------|--------|----------|
| `npm run release` | 完整发版：bump + CHANGELOG + **说明预览** + 提交 + 打 tag + push + 部署 | 会（**间接**：打 tag → CI 自动 publish 并建 Release） |
| `npm run release:notes` | 只**渲染**一段 Release 说明文本（CHANGELOG 段落优先，git 兜底） | **不会**，纯只读，不发任何请求 |

`release:notes` 就是 `release` 第 ⑥ 步里那次预览的独立入口，用途：发版前单独核对措辞、事后排查
「Release 说明为空 / 少了提交」，或强制 `--source git` 与 CHANGELOG 版本比对。
它同时支持**「tag 尚未创建」的预览**（`--tag vX.Y.Z` 时若该 tag 不存在，自动按 `HEAD` 统计并提示）。

## 5. 三道闸，各自拦什么

| 闸 | 拦住的真实事故 |
|----|----------------|
| ① 版本一致性 | tag 与 `package.json` 版本不一致 → 发出去的东西和标签对不上 |
| ③ 离线自测 | 路径解析被改坏（默认 HOME 应等于包根）、时段守卫被「顺手放开」、schema 差异识别退化 |
| ④ 打包体检 | **`files` 是硬白名单，会绕过 `.gitignore`** —— 实测曾把 `db/.env`（Cloudflare 令牌）与 `db/local.db` 打进 tarball |

体检脚本自带**守门规则自检**（10 条必拦 + 6 条必放行）：这类正则白名单最怕被后人放宽（删掉 `.env` 那条就再也不报警），所以正反例都钉在脚本里，每次运行先自检。

## 6. 踩过的坑（都已在代码里留注释）

| 坑 | 现象 | 修法 |
|----|------|------|
| `npm pack --json` 的形状 | 是**以包名为键的对象**，不是数组；按 `[...]` 截取会切进 `files` 数组 → `JSON.parse` 失败 | 直接解析，失败再退化为「首个 `{` 到末个 `}`」 |
| Windows 下 spawn `npm.cmd` | `spawnSync npm.cmd EINVAL` | `shell: true`（参数是常量，无注入面） |
| `changelogen` 的 `repo` | 配了 `{ provider, repo }`（**缺 `domain`**）→ 它跳过推断，拼出 **81 个** `https://undefined/...` 死链 | 删掉该键，交给 `package.json.repository` 推断；已用 `loadChangelogConfig()` 实测确认 `domain: github.com` |
| 首次发布没有上一版 tag | `git describe <tag>^` 失败并打脏 stderr | `git()` 支持 silent 模式，首次发布视为正常分支 |
| 自测泄漏临时目录 | `paths.selftest.js` 每跑一次留 2 个目录 | 收尾清理，连跑两次残留为 0 |

## 7. 验证清单

- [x] `npm pack --dry-run` 白名单内**无** `db/.env`、`db/local.db`、`notify_config.json`、`eod/data/eod-*.json`
- [x] 必需文件齐全（两个 bin、`paths.js`、`db/schema.sql`、`eod/tail_screener.js` 等）
- [x] 依赖于锁定文件一致 → `npm ci` 不会报 out of sync
- [x] `release-notes.mjs` 两种来源（CHANGELOG / git）都能出稿，链接指向 `github.com`
- [x] workflow YAML 可解析；`permissions` 含 `contents: write` + `id-token: write`
- [ ] **首次真实发布**：打 tag → CI 全绿 → npm 页面可见 → Release 说明含全部提交（待执行）

## 8. 与既有项目的一致性

| 项 | 本项目 | `yyds` | `uuid-nanoid` | `enhance-axios` | `change-image-suffix` |
|----|--------|--------|---------------|-----------------|----------------------|
| 触发 | `push.tags v*.*.*` | `v*` | `v*` | `v*` | `v*` |
| Node | 24 | 24 | 24 | 24 | 24 |
| 建 Release | `softprops/action-gh-release@v2` | v2 | v2 | v2 | v1 |
| 令牌 | `secrets.NPM_TOKEN` | 同 | 同 | 同 | 同 |
| 发布命令 | `npm publish --provenance --access public` | `--access public` | `npm publish` | `--provenance --access public` | `--access public` |
| 说明来源 | CHANGELOG → git 兜底 | `scripts/changelog.js` | CHANGELOG（awk） | CHANGELOG → git 兜底 | CHANGELOG（awk） |

**本仓库额外补的三道闸**（其它项目没有）：版本一致性校验、离线自测、打包体检 + 守门规则自检。
值得回填到其它项目——`npm publish` 前跑一次白名单体检，是防凭据泄露成本最低的一步。

## 9. 已知遗留（需你拍板）

| # | 事项 | 现状 | 影响 |
|---|------|------|------|
| 1 | **首次运行脚手架（Phase 1）** | 空目录里跑 `npx candidate-pool` 会因缺 `candidates.json` 直接报错退出 | 包的「开箱可用」还差这一步；建议空目录自动生成 观察池 + 行业映射 + `.env` 模板 |
| 2 | `scripts/release.mjs` 末尾仍 `npm run deploy --prefix worker` | 与 2026-09-20「独立 Worker 退出发布流程」的决定不一致 | 本机 `npm run release` 可能在最后一步失败（tag 已推、npm 已发，只是本地报错）；需确认是否删掉该步 |
| 3 | 定时任务若改用 `npx` 运行 | 现在都直接调仓库内脚本路径 | 必须 pin 版本（`npx candidate-pool@1.2.0`），否则阈值/口径会在不知情的情况下随最新版漂移 |
| 4 | 快照格式向后兼容 | 包内不带 `data/snapshot-*.json` | 新用户需先跑一次 live 才有快照；离线模式在空目录不可用 |
