# 尾盘选股 · 计划

> 📌 **状态：已全部完成（P0–P6）并上线**。本文是**计划基线**，保留原始阶段划分，**不回改**。
> 实际落地的调度与调研结果（含 GitHub cron 实测延迟）见 [`05-复盘.md`](05-复盘.md)。

> **一句话**：按「先能跑 → 再筛选 → 再排序 → 再通知 → 再攒样本」的顺序推进，每阶段都有可验证的交付物，**待你确认需求后开工**。

## 阶段划分

| 阶段 | 目标 | 交付物 | 验证方式 |
|------|------|--------|----------|
| **P0** 骨架 | 最小可运行闭环 | `tail_screener.js` 入口 + `tail.config.js` 阈值配置 + 参数（`--now` / `--date` / `--offline`）；交易日与时段判断；全市场快照抓取 | 非交易日直接跳过；交易日手动跑通并打印样本数与涨幅分布 |
| **P1** 初筛 | 按尾盘口径过滤 | FR-03 过滤实现（含 ST / 停牌 / 一字板 / 涨停 / 新股排除）+ 日K支撑字段（量比、前高） | 候选清单不含不可买标的；缺字段项标记 `data_gap` 且不入选 |
| **P2** 分时 + 打分 | 可解释的强度分 | 分时取 14:30–14:50 段；6 项分项得分 + 总分 | 抽 5 只人工核对分项得分是否合理 |
| **P3** 分组比较 | 同类里挑最优 | 板块分组 + 组内排序 + 「组内最优」标记 + 全市场 TOP 10 + 同质去重提示 | 每组前 3 排序正确、组内最优唯一 |
| **P4** 报告 + 通知 | 能看、能收到 | 自包含 HTML（双语切换，复用现有样式）+ 复用 `notify.js`（微信 + 邮箱） | 手机打开排版正常；微信与邮箱均收到且含报告入口 |
| **P5** 落库 + 回填 | 攒复盘底座 | `tail_run` / `tail_pick`（先落本地 JSON）+ 收盘后回填收盘价 / 次日表现 | 同交易日重复运行不产生重复记录；回填能与行情核对一致 |
| **P6** 调度与发布 | 双通道可执行 | GitHub Actions workflow（06:40 UTC 触发 + 程序内等到 14:50 + 上传产物）+ 本地 Windows 计划任务；可选发布报告 | 两条通道各成功跑通一次；Actions 产物可下载 |

## 依赖顺序

| 前置 | 说明 |
|------|------|
| P0 → P1 | 没有快照就无法定阈值 |
| P1 → P2 | 分时校验只对初筛通过的票做（否则请求量过大） |
| P2 → P3 | 排序依赖总分 |
| P3 → P4 | 报告结构依赖分组结果 |
| P4 → P5 | 落库字段需先定稿 |
| P5 → P6 | 回填逻辑确认后再上定时 |

## 里程碑验收（对应 `01-需求.md` 第 5 节）

| 里程碑 | 验收点 |
|--------|--------|
| M1（P0–P2） | 14:50 能产出带分项得分、且不含不可买标的的候选清单 |
| M2（P3–P4） | 报告按板块分组、组内有序，微信与邮箱都收到 |
| M3（P5–P6） | 结果落库 + 收盘回填，两条调度通道跑通 |
| M4（上线后 2–4 周） | 用积累样本做首轮复盘，回看阈值与权重是否要调 |

## 工作量提示

| 阶段 | 复杂度 | 说明 |
|------|--------|------|
| P0–P2 | 中 | 主要是复用现有抓取/重试逻辑，新增尾盘口径 |
| P3–P4 | 中 | 分组与报告排版，可大量复用现有 HTML 生成 |
| P5–P6 | 中高 | 落库与双通道调度，GitHub cron 时区/延迟需实测 |
